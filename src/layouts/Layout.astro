---
import BaseHead from "@shared/components/BaseHead.astro";
import ButtonToTop from "@shared/components/ButtonToTop.astro";
import CookiesBanner from "@shared/components/CookiesBanner/CookiesBanner";
import Footer from "@shared/components/Footer/Footer.astro";
import Header from "@shared/components/Header/Header.astro";
import MobileMenu from "@shared/components/MobileMenu/MobileMenu.astro";
import ThemeIcon from "@shared/components/ThemeIcon.astro";

const { title, description, image, class: ClassList, ...rest } = Astro.props;
const path = Astro.url.pathname;
const slug = Astro.url.pathname.split("/").pop();
---

<html lang="es">
  <head>
    <BaseHead title={title} description={description} image={image} />
    <!-- Google tag (gtag.js) -->
    <script
      is:inline
      defer
      async
      src="https://www.googletagmanager.com/gtag/js?id=G-4HD0MHWJHW"></script>
    <slot name="head" />
    <script
      is:inline
      async
      src="https://pagead2.googlesyndication.com/pagead/js/adsbygoogle.js?client=ca-pub-3828278469742835"
      crossorigin="anonymous"></script>
  </head>
  <body>
    {
      path == "/support" ? null : path ==
        `/support/contributor/${slug}` ? null : (
        <ThemeIcon />
      )
    }
    <ButtonToTop />
    <Header />
    <MobileMenu />
    <slot name="hero" />
    <main class={ClassList} {...rest}>
      <slot />
    </main>
    <Footer />

    <CookiesBanner client:load />
  </body>
</html>

<style is:global lang="scss">
  main {
    margin: 0 auto;
    width: 100%;
    font-size: 20px;
    line-height: 1.6;
  }
</style>
